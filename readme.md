My `.vimrc` should look something like this, depending on the system:

    source ~/dotfiles/vim/settings/index.vim
    source ~/dotfiles/vim/settings/ubuntu.vim

Do a simple `ln -s` for the other dotfiles.
